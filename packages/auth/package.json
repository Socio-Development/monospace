{
  "name": "@socio-development/auth",
  "version": "0.0.1",
  "description": "SDK for Socio authentication",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "format": "prettier --ignore-path ../../.prettierignore --config ../../.prettierrc --write ."
  },
  "publishConfig": {
    "main": "lib/index.js",
    "types": "lib/index.d.ts"
  },
  "devDependencies": {
    "prettier": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
