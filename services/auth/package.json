{
  "name": "auth-service",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "dev": "op run --env-file=../../.env -- tsx --watch src/main.ts",
    "format": "prettier --ignore-path ../../.prettierignore --config ../../.prettierrc --write .",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "express": "catalog:"
  },
  "devDependencies": {
    "@types/express": "catalog:",
    "@types/node": "catalog:",
    "prettier": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
