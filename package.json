{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "dev": "lerna run dev --parallel",
    "format": "lerna run format",
    "lint": "lerna run lint",
    "test": "lerna run test --parallel"
  },
  "engines": {
    "pnpm": ">=10"
  },
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}
